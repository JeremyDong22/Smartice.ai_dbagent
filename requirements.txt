# Dependencies for the Supabase Multiagent system
langgraph>=0.0.25
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-google-genai>=0.0.7
langchain-community>=0.0.16
supabase>=2.3.1 # 如果你的 utils.py 等直接使用了 supabase client
openai>=1.6.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.9 # 用于连接 PostgreSQL

# Web framework for API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sse-starlette>=1.8.0 # For efficient Server-Sent Events

# WSGI/ASGI Server for Production
gunicorn>=21.2.0 # <-- 添加 Gunicorn